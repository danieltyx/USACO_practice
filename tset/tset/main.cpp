#include<bits/stdc++.h>
using namespace std;
map<pair<int,int>,pair<int,int> > con;
int main()
{
 
    vector<int> v;
    sort(v.begin(), v.end());
    v.erase(unique(v.begin(), v.end()), v.end());
    
    for(int i=1)
    return 0;
}
